name: web_portfolio
description: Flutter Web Portfolio.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.1 <3.0.0"

dependencies:
  cached_network_image: ^3.2.2
  carousel_slider: ^4.1.1
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_icons_null_safety: ^1.1.0
  google_fonts: ^3.0.1
  http: ^0.13.5
  provider: ^6.0.3
  responsive_framework: ^0.2.0
  scrollable_positioned_list: ^0.3.5
  shared_preferences: ^2.0.15
  smooth_page_indicator: ^1.0.0+2
  url_launcher: ^6.1.6

dev_dependencies:
  build_runner: null

  flutter_gen_runner: null
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
